/**
 * Must match `INSTRUCTIONS_SERVERS_PLACEHOLDER` in `src/gateway/dispatch/initialize.ts`.
 * Substituted at MCP initialize with e.g. `["server-a","server-b"]` or `["none"]`.
 */
export const INSTRUCTIONS_SERVERS_PLACEHOLDER = '{{SERVERS}}' as const
