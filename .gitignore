implementation
.claude
gateway.json
.cursor 
AGENTS.md
CLAUDE.md
.sisyphus
.codex
bench

# Node
node_modules/
dist/
.env

# Local config (copy from *.example.json)
config/gateway.json
config/bootstrap.json

# Coverage
coverage/

# Build artifacts
*.tsbuildinfo
.gitnexus

# SQLite persistence (WAL sidecars are local/ephemeral)
*.db-shm
*.db-wal
data/

# Ephemeral SQLite + bootstrap from restart integration tests (created in beforeAll, removed in afterAll)
test/integration/__tmp_restart__/
bench/datasets/local/
bench/reports/

